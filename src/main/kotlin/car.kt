fun car(){
open abstract class Car(ID: ArrayList<Int>) : Vehicle( ) {

        override fun entrancePrice(price_Vehicle: Int) {
            entrancePrice(50000)
        }

        override fun hourPrice(price_Hour: Int) {
            hourPrice(20000)
        }
    }}
